<template format="4"
        revision="9"
        name="MVM Kotlin Feature"
        description="Creates new MVVM classes - ViewModel, Fragment, layout">

    <category value="Other"/>

    <parameter id="subpackageName"
        name="Feature package name"
        type="string"
        constraints="class|unique|nonempty"
        default="mvvmfeature"
        help="The name of the package that this feature will reside in"/>

    <parameter id="className"
        name="Feature name"
        type="string"
        constraints="class|unique|nonempty"
        default="MvvmFeature"
        help="The name of the feature that requires MVVM classes"/>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
