<template format="4"
        revision="9"
        name="MVP RxJava Feature"
        description="Creates new MVP classes - PresenterFactor, Presenter, View and Contract between the two">

    <category value="Other"/>

    <parameter id="subpackageName"
        name="Feature package name"
        type="string"
        constraints="class|unique|nonempty"
        default="mvpfeature"
        help="The name of the package that this feature will reside in"/>

    <parameter id="className"
        name="Feature name"
        type="string"
        constraints="class|unique|nonempty"
        default="MvpFeature"
        help="The name of the feature that requires MVP views"/>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>